import { type DefaultTheme } from "styled-components";
import colors from "styles/themes/lightPinkTheme/colors";
import formats from "styles/defaultTheme/formats";
import sizes from "styles/defaultTheme/sizes";

const lightPinkTheme: DefaultTheme = {
  colors,
  formats,
  name: "Light Pink",
  sizes,
};

export default lightPinkTheme;
