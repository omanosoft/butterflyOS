import type { DefaultTheme } from "styled-components";
import colors from "styles/turquoiseTheme/colors";
import formats from "styles/turquoiseTheme/formats";
import sizes from "styles/turquoiseTheme/sizes";

const turquoiseTheme: DefaultTheme = {
  colors,
  formats,
  name: "Turquoise",
  sizes,
};

export default turquoiseTheme;
